# Capturas pendientes: Apps Script

Las de PulsoFlow ya están (`pulsoflow-checkins.png` y `pulsoflow-miembros.png`,
con los datos de los socios tapados). Faltan las dos de Apps Script, que
necesitan una sesión de Google iniciada.

**Antes de sacarlas:** que no entre en la foto ningún dato de un alumno real. Los
logs de las corridas nombran alumnos y mails, así que conviene elegir una
ejecución cuyo resumen no los tenga, o recortar.

| # | Qué capturar | Dónde | Va en |
|---|---|---|---|
| 1 | La lista de activadores del proyecto, donde se ven los 7 con su frecuencia. | Apps Script → Activadores. | `extraccion-pulsoflow.md`, sección "Cómo se ve corriendo". |
| 2 | El log de una ejecución de `syncCheckins` terminada, con su "Check-ins procesados: N". | Apps Script → Ejecuciones. | Lo mismo, al lado de la anterior. |

Guardarlas como `docs/media/apps-script-triggers.png` y
`docs/media/apps-script-log.png`, y reemplazar el comentario `<!-- PENDIENTE ...
-->` del final de `extraccion-pulsoflow.md` por las dos imágenes.
